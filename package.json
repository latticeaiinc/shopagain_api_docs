{
  "name": "acme-api",
  "version": "1.0.0",
  "dependencies": {
    "@redocly/cli": "1.0.0-beta.123"
  },
  "private": true,
  "scripts": {
    "start": "redocly preview-docs",
    "build": "redocly bundle -o dist",
    "bundle": "redocly bundle -o public/dist.json",
    "builddoc": "redocly build-docs --output=index.html",
    "test": "redocly lint"
  }
}
